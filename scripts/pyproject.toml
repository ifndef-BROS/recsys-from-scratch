[project]
name = "recsys-scripts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "sentence-transformers",
    "pandas",
    "numpy",
    "scikit-learn",
    "cornac",
    "implicit",
    "tqdm",
    "psutil>=7.2.2",
]
